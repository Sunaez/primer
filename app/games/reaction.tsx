import { View, Text } from 'react-native';

export default function ReactionGame() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ fontSize: 24 }}>Reaction Game</Text>
    </View>
  );
}
